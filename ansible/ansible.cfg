# Fichier de configuration pour Ansible

[defaults]
inventory      = inventory.ini
host_key_checking = False
retry_files_enabled = False
stdout_callback = debug
remote_tmp = /tmp

[privilege_escalation]
become = False # Désactive l'élévation de privilèges par défaut