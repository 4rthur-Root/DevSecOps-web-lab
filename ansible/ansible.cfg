# Configuration file for Ansible

[defaults]
inventory      = inventory.ini
host_key_checking = False
retry_files_enabled = False
stdout_callback = debug
remote_tmp = /tmp

[privilege_escalation]
become = False # Disables privilege escalation by default